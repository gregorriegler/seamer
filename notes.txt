- cglib does not work with closures like lambdas do. maybe simple java proxy will?
- SRP: split seamer into seamer into two parts, one of which will persist seam and record invocations, and another one that will just execute recorded invocations?
- degenerate test: file does not contain pair but something else